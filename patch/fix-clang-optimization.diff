diff --git a/include/libbb.h b/include/libbb.h
index 01cdb1b..b7483e0 100644
--- a/include/libbb.h
+++ b/include/libbb.h
@@ -380,9 +380,12 @@ struct BUG_off_t_size_is_misdetected {
  * this optimization.
  */
 #ifndef BB_GLOBAL_CONST
-# define BB_GLOBAL_CONST const
+#if defined(__clang_major__) && __clang_major__ >= 9
+	# define BB_GLOBAL_CONST
+#else
+	# define BB_GLOBAL_CONST const
+#endif
 #endif
-
 #if defined(errno)
 /* If errno is a define, assume it's "define errno (*__errno_location())"
  * and we will cache it's result in this variable */
